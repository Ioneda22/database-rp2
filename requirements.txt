# --- construção da base (etapas 1 a 4 do main.py) ---
pandas>=2.0
openpyxl>=3.1        # lê os .xlsx da SSP em streaming
xlrd>=1.2,<2         # lê o .xls legado (BIFF) do IEGM; xlrd 2.x removeu suporte
sidrapy>=0.1.4
requests>=2.31

# --- modelagem (seções 3.3 e 3.4 do artigo) ---
scikit-learn>=1.3    # K-means, hierárquico, DBSCAN e métricas de validação
scipy>=1.10          # Kruskal-Wallis / ANOVA
matplotlib>=3.7
seaborn>=0.13

# --- mapas coropléticos (seção 3.4) ---
# precisa também da malha municipal do IBGE, unida por codigo_ibge
geopandas>=0.14
